plugins { id 'com.android.application' }

android {
    namespace 'com.exgine.firstlight'
    compileSdk 35
    defaultConfig {
        applicationId 'com.exgine.firstlight'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '1.0.0'
        ndkVersion '27.2.12479018'
        externalNativeBuild { cmake { arguments '-DANDROID_STL=c++_static', '-DEXGINE_BUILD_DEMO=OFF', '-DEXGINE_BUILD_TESTS=OFF' } }
    }
    buildTypes {
        release { minifyEnabled false; debuggable false }
        debug { debuggable true }
    }
    sourceSets {
        main { assets.srcDirs = ['../../../examples'] }
    }
    externalNativeBuild { cmake { path file('../CMakeLists.txt'); version '3.22.1' } }
}
